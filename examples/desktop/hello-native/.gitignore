build/
dist/
# Native binaries are build output of build_native.sh (gcc/clang) or
# build_native.ps1 (MSVC), not source.
binaries/
# MSVC intermediates if cl is ever run in-place (build_native.ps1 uses a temp dir).
*.obj
*.lib
*.exp
# Generated by `kivyforge lock`; regenerate with `kivyforge lock -p <platform>`.
pylock.*.toml
