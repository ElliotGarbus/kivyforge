"""The kivyforge-owned Android bootstrap (android/05).

Templates extracted from the Phase-0 prototype that proved the load model
on-device (docs/design/dev/android-loadmodel-findings.md) — never edited
against an unproven contract. ``render`` parameterizes them per project;
``contract`` enforces the pyjnius ``invoke0`` matched-pair gate.
"""
