name: kivyforge

on:
  # `push` is limited to main so a branch with an open PR does not run the whole
  # matrix twice. It used to fire on every branch, which doubled the cost of
  # every push and — because the desktop gates' `lock --check` queries GitHub's
  # API — doubled the rate-limit pressure too. That is not hypothetical: two
  # concurrent runs of the same commit are what produced the 403 on 2026-09-22
  # (test-matrix.md §7). Branch coverage comes from `pull_request`.
  push:
    branches: [main]
  pull_request:
  # Manual re-vendor of the Windows launcher when the runner's MSVC toolset drifts
  # (see the revendor_launcher job).
  workflow_dispatch:

jobs:
  lint:
    name: Lint (ruff + pyright)
    runs-on: ubuntu-latest
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.x'
    - name: Install dev dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: ruff check
      run: ruff check kivyforge tests
    - name: ruff format --check
      run: ruff format --check kivyforge tests
    # Scope and settings live in pyrightconfig.json, including
    # pythonPlatform: All -- without it this ubuntu job would fail on the
    # Windows-only registry probe that Windows hosts analyse cleanly.
    - name: pyright
      run: pyright

  # The Android bootstrap vendors SDL's Java glue paired with the libSDL*.so
  # the kivy-mobile-wheels repo ships (android/05 §matched pair;
  # kivyforge/platforms/android/bootstrap/contract.py). check_sdl_glue_contract
  # catches a version-stamp mismatch at build time, but only after the fact;
  # this job catches the two repos' vendored copies silently diverging in
  # *content* the moment either one changes.
  sdl_glue_sync:
    name: SDL glue sync (kivy-mobile-wheels)
    runs-on: ubuntu-latest
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.x'
    - name: Verify vendored SDL glue matches kivy-mobile-wheels
      run: python scripts/check_sdl_glue_sync.py

  package:
    name: Package (build + twine)
    runs-on: ubuntu-latest
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.x'
    - name: Install build tooling
      run: |
        python -m pip install --upgrade pip build twine
    - name: Build sdist and wheel
      run: python -m build
    - name: Twine check
      run: twine check dist/*

  # End-user documentation (roadmap item 6). --strict turns a broken internal
  # link or a nav entry with no page into a build failure, so docs cannot merge
  # broken. No network: mkdocs build reads only the repo, and the generated
  # tables come from kivyforge's own code (docs/hooks/kf_generated.py). The
  # link-checker, which does need the network, is deliberately not run here.
  docs:
    name: Docs (mkdocs build --strict)
    runs-on: ubuntu-latest
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.x'
    - name: Install kivyforge with the docs extra
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[docs]"
    - name: Build the site (strict)
      run: mkdocs build --strict

  unit_tests:
    name: Unit tests (Ubuntu)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.13', '3.14']
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install kivyforge and test dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: Run pytest with coverage
      run: |
        pytest --cov=kivyforge --cov-report=term-missing --cov-report=xml

  windows_tests:
    name: Unit tests (Windows)
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: ['3.13', '3.14']
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Enable Windows symlink creation (Developer Mode)
      # git config core.symlinks does NOT grant the OS privilege that os.symlink
      # needs; without it the requires_symlinks tests self-skip. Enabling
      # Developer Mode grants SeCreateSymbolicLinkPrivilege to non-elevated
      # processes, so the pytest step below (a fresh process) can create symlinks.
      run: reg add "HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock" /t REG_DWORD /f /v "AllowDevelopmentWithoutDevLicense" /d "1"
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install kivyforge and test dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: Run pytest with coverage
      # KIVYFORGE_REQUIRE_SYMLINKS makes the suite fail loudly if the symlink
      # privilege is missing, so requires_symlinks tests can never silently skip
      # in CI (see tests/conftest.py).
      env:
        KIVYFORGE_REQUIRE_SYMLINKS: '1'
      run: pytest --cov=kivyforge --cov-report=term-missing --cov-report=xml

  windows_launcher:
    name: Launcher reproducibility + vendored assets (Windows)
    # arm64: this job (and revendor_launcher below) build/verify only
    # launcher-amd64.exe. Adding win_arm64 means also building/verifying
    # launcher-arm64.exe — cross-compile on this same x64 runner via
    # `vcvarsall x64_arm64` (add the VC.Tools.ARM64 component), or add an arm64
    # runner; a build-arch matrix is the natural shape. See arm64-windows.md §10.
    # Skipped on manual re-vendor runs: this job would only fail on the stale
    # vendored files it checked out before its sibling committed. The equivalent
    # gate for those runs is revendor_verify, which runs these same steps on a
    # fresh runner against the commit revendor_launcher just pushed.
    if: github.event_name != 'workflow_dispatch'
    runs-on: windows-latest
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.x'
    - name: Install kivyforge
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: Verify vendored rcedit matches its pinned SHA-256
      run: python -m kivyforge.platforms.windows.vendor.fetch_rcedit --check
    - name: Rebuild the launcher from source and byte-compare (reproducibility)
      # Cross-runner byte-identity comes from the linker's
      # /EMITTOOLVERSIONINFO:NO (no PE "rich header", so the serviced cl/link
      # build is not stamped into the image — VCTOOLSVERSION does NOT pin that;
      # see build_launcher.py's docstring). The TOOLSET.txt pin (e.g. 14.51.36231
      # / 10.0.26100.0, loaded via vcvarsall — not "latest") holds code
      # generation steady on top of it. If this fails with "cl not found" / a
      # vcvars error the runner image dropped that exact pair; if it fails with a
      # byte mismatch the C source or the compile/link flags changed without
      # re-vendoring. Either way: Actions → Run workflow (`revendor_launcher`)
      # to re-pin against the runner's current defaults.
      run: python -m kivyforge.platforms.windows.launcher.build_launcher verify
    - name: Exercise the launcher end-to-end (spawn/argv/exit-code/env)
      # These tests self-skip without MSVC, which on this job would be a green run
      # that tested nothing — the same failure mode KIVYFORGE_REQUIRE_SYMLINKS
      # exists for. This runner has MSVC, so say so and let a missing one fail.
      env:
        KIVYFORGE_REQUIRE_TOOLCHAIN: '1'
      run: pytest tests/platforms/windows/test_launcher_exe.py -v --no-cov

  revendor_launcher:
    name: Re-vendor launcher (manual, on toolset drift)
    # Manual-only: run from the Actions tab when windows_launcher fails because the
    # runner image dropped the pinned MSVC toolset. Rebuilds the launcher with the
    # runner's current (in-support) toolset, self-checks reproducibility, and
    # commits the refreshed binary + SHA256SUMS + TOOLSET.txt back to the branch.
    # The self-check here is same-runner only; revendor_verify is the real gate.
    if: github.event_name == 'workflow_dispatch'
    runs-on: windows-latest
    permissions:
      contents: write
    outputs:
      # The commit revendor_verify must check out — the one this job pushed, or
      # the checked-out commit when the launcher was already up to date.
      sha: ${{ steps.commit.outputs.sha }}
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.x'
    - name: Install kivyforge
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: Rebuild the launcher with the runner's current MSVC toolset
      shell: pwsh
      run: |
        # Drop the stale pin so build_launcher resolves the runner's default
        # (current, in-support) MSVC + Windows SDK and records both in
        # TOOLSET.txt.
        Remove-Item kivyforge\platforms\windows\vendor\TOOLSET.txt -ErrorAction SilentlyContinue
        python -m kivyforge.platforms.windows.launcher.build_launcher build
    - name: Verify the re-vendored launcher is reproducible on this runner
      # Same-machine check only — it catches a broken build before committing,
      # but proves nothing about cross-runner drift (the failure mode that
      # /EMITTOOLVERSIONINFO:NO exists to kill). revendor_verify does that.
      run: python -m kivyforge.platforms.windows.launcher.build_launcher verify
    - name: Commit and push the re-vendored launcher (if changed)
      id: commit
      shell: bash
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git add kivyforge/platforms/windows/vendor/launcher-amd64.exe \
                kivyforge/platforms/windows/vendor/SHA256SUMS \
                kivyforge/platforms/windows/vendor/TOOLSET.txt
        if git diff --cached --quiet; then
          echo "Launcher already reproducible with the current toolset; nothing to commit."
        else
          # TOOLSET.txt is multi-line (MSVC + Windows SDK); flatten for the subject.
          toolset="$(grep -v '^#' kivyforge/platforms/windows/vendor/TOOLSET.txt | tr '\n' ' ' | sed 's/[[:space:]]*$//')"
          git commit -m "chore(windows): re-vendor launcher for ${toolset}"
          git push origin "HEAD:${GITHUB_REF_NAME}"
        fi
        echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
    - name: Upload re-vendored launcher assets (fallback if push is blocked)
      uses: actions/upload-artifact@v6
      with:
        name: vendored-launcher
        path: |
          kivyforge/platforms/windows/vendor/launcher-amd64.exe
          kivyforge/platforms/windows/vendor/SHA256SUMS
          kivyforge/platforms/windows/vendor/TOOLSET.txt

  revendor_verify:
    name: Verify the re-vendored launcher (fresh runner)
    # revendor_launcher pushes with the default GITHUB_TOKEN, and GitHub does not
    # start new workflow runs from those pushes (recursion guard) — so a re-vendor
    # commit lands with its reproducibility gate never having run, until someone
    # notices and pushes an empty commit. This job closes that hole: a separate
    # job means a **different runner VM**, which is exactly the cross-machine
    # comparison the gate is for (the same-job check above cannot see that drift).
    needs: revendor_launcher
    if: github.event_name == 'workflow_dispatch'
    runs-on: windows-latest
    steps:
    - name: Checkout the re-vendored commit
      uses: actions/checkout@v6
      with:
        # The commit revendor_launcher pushed, not the one that triggered this
        # run. If the push was blocked this ref will not exist on the remote and
        # this job fails loudly — grab the `vendored-launcher` artifact instead.
        ref: ${{ needs.revendor_launcher.outputs.sha }}
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.x'
    - name: Install kivyforge
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: Rebuild the launcher from source and byte-compare (reproducibility)
      run: python -m kivyforge.platforms.windows.launcher.build_launcher verify
    - name: Exercise the launcher end-to-end (spawn/argv/exit-code/env)
      # The re-vendored binary is what ships; prove it runs, not just that its
      # bytes reproduce. Nothing else exercises it on a workflow_dispatch run.
      env:
        KIVYFORGE_REQUIRE_TOOLCHAIN: '1'
      run: pytest tests/platforms/windows/test_launcher_exe.py -v --no-cov

  windows_signing:
    name: Authenticode sign + verify (self-signed, Windows)
    runs-on: windows-latest
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.x'
    - name: Install kivyforge
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: Self-signed sign -> verify loop through SigntoolSigner
      shell: pwsh
      run: |
        # Put the Windows SDK's signtool on PATH so shutil.which() finds it
        # (this is exactly what SigntoolSigner.sign resolves at runtime).
        $signtool = Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin\*\x64\signtool.exe" |
          Sort-Object FullName -Descending | Select-Object -First 1
        if (-not $signtool) { throw "signtool.exe not found in the Windows SDK" }
        $env:PATH = "$($signtool.Directory);$env:PATH"

        # A code-signing cert in CurrentUser\My (the store SigntoolSigner uses
        # without /sm); trust its public cert so `signtool verify /pa` passes.
        $cert = New-SelfSignedCertificate -Type CodeSigningCert `
          -Subject "CN=kivyforge CI" -CertStoreLocation Cert:\CurrentUser\My
        $tp = $cert.Thumbprint
        Export-Certificate -Cert $cert -FilePath ci.cer | Out-Null
        Import-Certificate -FilePath ci.cer -CertStoreLocation Cert:\LocalMachine\Root | Out-Null

        # Sign a copy of the vendored launcher via our own SigntoolSigner, then
        # verify the resulting Authenticode signature + timestamp.
        #
        # The verification goes through tests/platforms/windows/
        # test_authenticode.py rather than a bare `signtool verify /pa`, which
        # checked only the exit code. The driver asserts the verification
        # counts and the RFC3161 timestamp as well, and running it here is what
        # puts the report parser itself under CI — no job builds a Windows
        # bundle yet, so this is the only place that exercises it
        # (test-matrix.md §5.1, §5.3).
        Copy-Item kivyforge\platforms\windows\vendor\launcher-amd64.exe signed.exe
        python -c "from pathlib import Path; from kivyforge.platforms.windows.signing import SigntoolSigner; SigntoolSigner('$tp', 'http://timestamp.digicert.com', 'current_user').sign([Path('signed.exe')])"
        $env:KIVYFORGE_REQUIRE_TOOLCHAIN = '1'
        pytest tests/platforms/windows/test_authenticode.py -q --no-cov `
          --windows-signed-exe signed.exe

  # The Linux desktop gate: `kivyforge package -p linux` end to end against a
  # real appimagetool, then the T3 artifact pass over what it produced. Until
  # this job existed, no CI job ran `kivyforge build` or `package` for ANY
  # desktop target — Linux, macOS and Windows were all proven by local runs
  # logged in test-matrix.md §7 and nothing that repeats (§3.2).
  #
  # It builds a fixture, not an example. tests/fixtures/apps/linux-gate commits
  # its pylock.linux.toml, which examples/** deliberately never does; the
  # reasoning is in that tree's README and in test-matrix.md §5.3. The short
  # version: a gate needs the same inputs every run, so that red means
  # kivyforge changed rather than PyPI did.
  linux_appimage:
    name: Linux AppImage (package + T3)
    runs-on: ubuntu-latest
    env:
      APP_DIR: tests/fixtures/apps/linux-gate
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python 3.13
      # Pinned, per test-matrix.md §5.8: this is the sole CI proof of Linux, so
      # it must not test whichever minor the runner image happens to ship.
      #
      # Not pinned for the .pyc-magic check, which is a different mechanism
      # here than on Android: the Linux T3 driver asks the artifact's OWN
      # staged interpreter what magic it accepts (a native x86_64 build stages
      # an executable one), so that assertion is anchored to the bundle rather
      # than to this runner.
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
    - name: Install kivyforge
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: Doctor (Linux)
      # Gating, deliberately — no `continue-on-error` here. The reason
      # android_gradle waives its doctor step (a known-interim Kivy wheel it
      # reports as FAIL, test-matrix.md §5.9) has no Linux equivalent, and a
      # headless runner only costs a WARN: `check_display_session` returns
      # WARN, not FAIL, when DISPLAY and WAYLAND_DISPLAY are both unset, and
      # doctor's exit code reacts only to FAIL. Verified by running it with
      # both variables unset.
      working-directory: ${{ env.APP_DIR }}
      run: kivyforge doctor -p linux
    - name: Verify the committed lock still matches pyproject.toml
      # `lock --check` is meaningful here precisely because the lock is
      # committed — against a gitignored one it can only ever report "out of
      # date" (03-lockfile-concept.md §"Example-repo lock policy"). Nothing
      # else in CI exercises this verb against a real lock.
      working-directory: ${{ env.APP_DIR }}
      env:
        # The PBS runtime lookup queries GitHub's API, which is 60
        # requests/hour per IP unauthenticated; the token Actions
        # already provides raises that to 5000 and costs nothing.
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: kivyforge lock -p linux --check
    - name: Package the AppImage (T2)
      # No --update: the committed lock is the input. If this step starts
      # failing without the fixture having changed, a pinned wheel or the PBS
      # runtime moved at its URL, which is worth knowing loudly.
      working-directory: ${{ env.APP_DIR }}
      run: kivyforge package -p linux
    - name: Assert the AppImage is the artifact the build promised (T3)
      # strip_source and byte_compile both default to "release", which only
      # `package` and `run --release` reach, so --linux-stripped is correct
      # here and would be wrong for a `build`. This is the pass that
      # caught the shipped AppRun defect in test-matrix.md §7.
      env:
        KIVYFORGE_REQUIRE_TOOLCHAIN: '1'
      run: |
        image="$(ls "$APP_DIR"/dist/linux/*.AppImage)"
        pytest tests/platforms/linux/test_appimage_artifact.py -q --no-cov \
          --linux-appimage "$image" --linux-arch x86_64 --linux-stripped
    - name: Upload the AppDir listing on failure (for triage)
      if: failure()
      uses: actions/upload-artifact@v6
      with:
        name: linux-gate-appdir-listing
        # The AppDir itself is ~120 MB and mostly the embedded runtime; a file
        # listing plus the generated launcher answers every question triage
        # actually asks of it.
        path: |
          ${{ env.APP_DIR }}/build/linux/*.AppDir/AppRun
          ${{ env.APP_DIR }}/build/linux/*.AppDir/*.desktop

  # The Windows desktop gate, completing the set alongside linux_appimage and
  # macos_app: `kivyforge package -p windows` end to end, then the T3 pass over
  # the onedir bundle it produced, then the one thing neither of the other two
  # can do — sign a *built* launcher and verify it.
  #
  # Same fixture pattern as the other two (test-matrix.md §5.3): the project is
  # tests/fixtures/apps/windows-gate, which commits its pylock.windows.toml so
  # the gate builds identical inputs every run.
  windows_onedir:
    name: Windows onedir (package + T3 + signature)
    runs-on: windows-latest
    env:
      APP_DIR: tests/fixtures/apps/windows-gate
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python 3.13
      # Pinned for the §5.8 reason — this is the only CI job that builds a
      # Windows app, so it must not test whichever minor the image ships.
      # The .pyc-magic assertion is anchored to the bundle's own staged
      # interpreter rather than to this runner, same as the Linux job.
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
    - name: Install kivyforge
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: Doctor (Windows)
      # Gating, like the Linux and macOS jobs. With no
      # [tool.kivy.windows.signing] the signtool and certificate checks SKIP
      # rather than FAIL, so an unsigned fixture needs no waiver.
      working-directory: ${{ env.APP_DIR }}
      run: kivyforge doctor -p windows
    - name: Verify the committed lock still matches pyproject.toml
      working-directory: ${{ env.APP_DIR }}
      env:
        # The PBS runtime lookup queries GitHub's API, which is 60
        # requests/hour per IP unauthenticated; the token Actions
        # already provides raises that to 5000 and costs nothing.
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: kivyforge lock -p windows --check
    - name: Package the onedir bundle (T2)
      # No --update: the committed lock is the input, so a failure here means a
      # pinned wheel or the PBS runtime moved at its URL.
      working-directory: ${{ env.APP_DIR }}
      run: kivyforge package -p windows
    - name: Assert the bundle is the artifact the build promised (T3)
      # strip_source and byte_compile default to "release", which only
      # `package` and `run --release` reach, so --windows-stripped is correct
      # here and would be wrong for a `build`.
      shell: bash
      run: |
        bundle="$(ls -d "$APP_DIR"/dist/windows/*-amd64)"
        pytest tests/platforms/windows/test_onedir_artifact.py -q --no-cov \
          --windows-onedir "$bundle" --windows-arch amd64 --windows-stripped
    - name: Sign the built launcher and verify it (T3, signature)
      # test-matrix.md §5.1 asked for `signtool verify /pa` "on a *built app*
      # rather than on the vendored launcher". This is that: the launcher
      # inside the bundle the previous step just inspected.
      #
      # Signed here rather than by `kivyforge package` because Windows has no
      # ad-hoc floor the way macOS does — signtool needs a real certificate,
      # and the thumbprint of a per-run throwaway cert cannot go in the
      # fixture's pyproject.toml without changing pyproject_sha256 and
      # breaking the `lock --check` step above. So the job signs the built
      # artifact through kivyforge's own SigntoolSigner, exactly as
      # windows_signing already does for the vendored launcher, and then
      # verifies it through the same driver.
      shell: pwsh
      env:
        KIVYFORGE_REQUIRE_TOOLCHAIN: '1'
      run: |
        $signtool = Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin\*\x64\signtool.exe" |
          Sort-Object FullName -Descending | Select-Object -First 1
        if (-not $signtool) { throw "signtool.exe not found in the Windows SDK" }
        $env:PATH = "$($signtool.Directory);$env:PATH"

        # A throwaway code-signing cert in CurrentUser\My (the store
        # SigntoolSigner uses without /sm); its public cert is trusted so
        # `verify /pa`, which applies the full Authenticode policy, can pass.
        $cert = New-SelfSignedCertificate -Type CodeSigningCert `
          -Subject "CN=kivyforge CI windows-gate" -CertStoreLocation Cert:\CurrentUser\My
        Export-Certificate -Cert $cert -FilePath ci-gate.cer | Out-Null
        Import-Certificate -FilePath ci-gate.cer -CertStoreLocation Cert:\LocalMachine\Root | Out-Null

        $bundle = (Get-ChildItem "$env:APP_DIR\dist\windows" -Directory |
          Where-Object Name -like "*-amd64" | Select-Object -First 1).FullName
        $launcher = Join-Path $bundle "Windows Gate.exe"
        if (-not (Test-Path $launcher)) { throw "built launcher not found at $launcher" }

        python -c "from pathlib import Path; from kivyforge.platforms.windows.signing import SigntoolSigner; SigntoolSigner('$($cert.Thumbprint)', 'http://timestamp.digicert.com', 'current_user').sign([Path(r'$launcher')])"
        pytest tests/platforms/windows/test_authenticode.py -q --no-cov `
          --windows-signed-exe "$launcher"
    - name: Reproduce the MAX_PATH limit with long paths off (§5.6)
      # LongPathsEnabled is off on a default Windows install, which is what
      # most users run, but on for this runner and the dev box, so the limit
      # had never been observed. Switched off here, on a disposable VM, and
      # deliberately the LAST real step: every process started after it runs
      # with long paths off. The test moves the packaged bundle into folders of
      # chosen lengths and asserts the headroom `package` reports is exact:
      # the deepest file opens at that folder length and not one character
      # past it. It fails rather than skips if long paths are not really off.
      shell: pwsh
      run: |
        Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem' -Name LongPathsEnabled -Value 0 -Type DWord
        $bundle = (Get-ChildItem "$env:APP_DIR/dist/windows" -Directory | Where-Object Name -like '*-amd64').FullName
        python -m pytest tests/platforms/windows/test_long_paths_live.py -v -s --no-cov --windows-long-paths-off-bundle "$bundle"
    - name: Upload the bundle listing on failure (for triage)
      if: failure()
      uses: actions/upload-artifact@v6
      with:
        name: windows-gate-bundle-listing
        # The bundle is ~200 MB and mostly the embedded runtime; the launcher
        # and the generated bootstrap answer what triage actually asks.
        path: |
          ${{ env.APP_DIR }}/dist/windows/*-amd64/_kivyforge_bootstrap.py
          ${{ env.APP_DIR }}/dist/windows/*-amd64/*.exe

  # The Android gate that unit tests structurally cannot be: every generated
  # Gradle/CMake/Java/XML file is only a *string* until AGP, AAPT, the NDK, and
  # javac actually consume it. This job builds a real app end to end from its
  # committed lock — debug APK, then the release policy path (lintRelease + the
  # merged-manifest export) against a throwaway keystore. No emulator: nothing
  # here runs on-device, so the instrumented contract smoke test (android/06
  # `run --smoke`) is still a local/manual gate.
  android_gradle:
    name: Android build (Gradle, ${{ matrix.abi }})
    runs-on: ubuntu-latest
    strategy:
      # Both ABIs, since 2026-09-23. This used to build x86_64 only, on the
      # reasoning that "one ABI is enough to compile the launcher and prove
      # the load model; both would double the CMake work for no new signal".
      # The first half still holds; the second stopped being true once this
      # was the *only* direct Android T3 anywhere. x86_64 is the emulator's
      # ABI — arm64_v8a is the one every real phone runs, and test-matrix.md
      # §3.2 had it as "inherited, not direct" all along. The new signal is
      # concrete: its ELF machine, its per-ABI jniLibs, and the stray-ABI
      # check, none of which x86_64 exercises for it.
      #
      # fail-fast off so one ABI breaking still reports the other, which is
      # exactly the comparison worth having when an ABI-specific fault lands.
      fail-fast: false
      matrix:
        include:
          - abi: arm64_v8a
            android_abi: arm64-v8a
          - abi: x86_64
            android_abi: x86_64
    env:
      # The example this gate builds; its pylock.android.toml is committed, so
      # the job needs no Gradle-at-lock-time and no resolver network beyond
      # fetching the pinned artifacts.
      APP_DIR: examples/mobile/hello-android
      # Two spellings of the same ABI, deliberately both here. kivyforge's
      # --abi takes the wheel-tag form ([tool.kivy.android].abis: arm64_v8a),
      # while the T3 driver's --android-abi takes Android's own dashed
      # directory name (lib/arm64-v8a/). They are identical for x86_64, which
      # is why one variable sufficed while this job built only that.
      #
      # Passing the wheel-tag form to --android-abi fails *loudly*, not
      # silently — `unknown ABI 'arm64_v8a'; expected one of ['arm64-v8a',
      # 'x86_64']`, confirmed by running it. So this split is about the job
      # passing for the right reason, not about avoiding a false green.
      ABI: ${{ matrix.abi }}
      ANDROID_ABI: ${{ matrix.android_abi }}
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python 3.14
      # Must match [tool.kivy.android.python].version's minor: byte_compile
      # ("release" by default) only writes .pyc when an interpreter with the
      # target's magic number is available, and warns + ships source otherwise.
      # Pinning it here means the release step exercises the .pyc path.
      uses: actions/setup-python@v6
      with:
        python-version: '3.14'
    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: '17'
    - name: Set up the Android SDK
      # The action's default `packages` is "tools platform-tools", but the
      # legacy `tools` package is gone from Google's SDK repository, so
      # `sdkmanager tools` exits 1 and fails the step. Nothing here uses it;
      # cmdline-tools (installed by the action) is its replacement.
      uses: android-actions/setup-android@v4
      with:
        packages: platform-tools
    - name: Install kivyforge (with the Android extra)
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev,android]"
    - name: Install the pinned NDK, CMake, platform, and build-tools
      # The NDK version is kivyforge's own pin, emitted into the generated
      # app/build.gradle, so the one AGP resolves must be that one — not
      # whichever the runner image happens to ship. Read it from the source of
      # truth so a toolchain bump can't leave this workflow behind.
      #
      # build-tools is named explicitly to guarantee a floor: the release
      # signature step below resolves `apksigner` out of
      # <sdk>/build-tools/*/, and without this line that step's ability to run
      # at all would depend on the runner image still shipping one.
      #
      # It does not pin which one runs. `_find_apksigner` takes the newest
      # version directory, deliberately (it is what AGP resolves to), so on an
      # image that already ships a newer build-tools this install is the
      # fallback rather than the selection — the 2026-09-22 run used 37.0.0
      # from the image, not the 35.0.0 installed here.
      run: |
        ndk="$(python -c 'from kivyforge.platforms.android.toolchain import NDK_VERSION; print(NDK_VERSION)')"
        echo "Installing NDK ${ndk}"
        sdkmanager --install "ndk;${ndk}" "cmake;3.22.1" "platforms;android-35" \
          "build-tools;35.0.0"
    - name: Doctor (Android)
      # Gating, as of 2026-09-23 — the `continue-on-error: true` that used to
      # be here is gone, closing test-matrix.md §5.9. The waiver's stated
      # reason was that doctor reports the known-interim 4 KB-aligned Kivy
      # wheel as a FAIL, and that reason does not hold *at this point in the
      # job*: `_check_16k_alignment` runs only `if project_dir.is_dir()`,
      # meaning the *generated* project, which `build` below has not created
      # yet. So the check is not merely passing here, it is not registered.
      #
      # Checked rather than reasoned about — the 2026-09-23 run of this job
      # reports 0 FAIL and 2 WARN (no device attached, no AVD), and doctor's
      # exit code is `worst_status(results) is not Status.FAIL`, so WARNs do
      # not gate. What the waiver actually bought was that this job ignored
      # *every* diagnostic doctor produced, which is what §5.9 objected to.
      #
      # If a future change stages jniLibs before this step, the interim-wheel
      # FAIL becomes reachable again and this needs the allow-list §5.9
      # originally proposed rather than the waiver coming back.
      working-directory: ${{ env.APP_DIR }}
      run: kivyforge doctor -p android
    - name: Build the debug APK (assembleDebug)
      working-directory: ${{ env.APP_DIR }}
      run: kivyforge build -p android --debug --abi "$ABI"
    - name: Assert the debug APK is the artifact the build promised (T3)
      # Replaces three `unzip -l | grep` presence checks with the full T3 pass
      # (tests/artifact_checks.py): required entries, single-ABI ELF machine
      # match, no shared object stranded in the payload, and — for the release
      # step below — .pyc-only with a magic the shipped runtime can import.
      # Debug does not strip, so --android-stripped is deliberately absent here;
      # this run asserts the *source* payload shape instead.
      run: |
        apk="$(ls "$APP_DIR"/hello-android-android/app/build/outputs/apk/debug/*.apk)"
        pytest tests/platforms/android/test_apk_artifact.py -q --no-cov \
          --android-apk "$apk" --android-abi "$ANDROID_ABI"
    - name: Exercise the release policy path (lintRelease + merged manifest)
      # `package` is the only path that runs the curated lintRelease subset and
      # the merged-manifest policy pass, and both live in generated Groovy — so
      # a throwaway keystore is worth it to keep that code honest.
      working-directory: ${{ env.APP_DIR }}
      env:
        KIVYFORGE_KEYSTORE_PASSWORD: ci-not-a-secret
      run: |
        keytool -genkeypair -noprompt -keystore ci.keystore -alias ci \
          -storepass "$KIVYFORGE_KEYSTORE_PASSWORD" \
          -keypass "$KIVYFORGE_KEYSTORE_PASSWORD" \
          -keyalg RSA -keysize 2048 -validity 30 -dname "CN=kivyforge CI"
        kivyforge package -p android --abi "$ABI" \
          --keystore ci.keystore --key-alias ci
    - name: Assert the release APK is stripped and byte-compiled (T3)
      # The gate roadmap item 1 needed and did not have. byte_compile and
      # strip_source both default to "release", so this APK has been stripped and
      # byte-compiled all along — CI simply never looked. It asserts .pyc-only, an
      # `app/main.pyc` in the legacy sourceless layout, no __pycache__, and every
      # .pyc carrying the magic this job's pinned CPython writes (which is why the
      # setup-python pin above is load-bearing, not incidental).
      run: |
        apk="$APP_DIR/hello-android-android/app/build/outputs/apk/release/app-release.apk"
        pytest tests/platforms/android/test_apk_artifact.py -q --no-cov \
          --android-apk "$apk" --android-abi "$ANDROID_ABI" --android-stripped
    - name: Assert the merged release manifest says what pyproject.toml declared (T3)
      # `package` exported this manifest already, for the policy lint to read,
      # and the job then threw it away on success — the same "CI produced the
      # evidence and never looked at it" shape as item 1 (test-matrix.md §5.1).
      # Policy lints the *posture*; this checks the *content*: applicationId,
      # versionCode/versionName and min/target SDK (all injected by AGP out of
      # build.gradle, so no generation test can cover them), every declared
      # permission, feature, service and intent-filter, and that no manifest
      # placeholder survived unresolved.
      run: |
        manifest="$APP_DIR/hello-android-android/app/build/kivyforge/AndroidManifest-merged-release.xml"
        pytest tests/platforms/android/test_merged_manifest.py -q --no-cov \
          --android-merged-manifest "$manifest" --android-project "$APP_DIR"
    - name: Assert the release APK's signature verifies (T3)
      # `apksigner verify` at the project's own min_sdk, against the release
      # APK signed by the throwaway keystore above. Release-only on purpose:
      # a debug APK's signature is Gradle's, not the project's, so the
      # v1_signing comparison would be meaningless on one.
      #
      # KIVYFORGE_REQUIRE_TOOLCHAIN turns a missing `apksigner` into a failure
      # rather than a skip: this job installs build-tools explicitly above, so
      # its absence is a broken workflow, and a green run that verified no
      # signature at all is exactly the silent-skip failure this tier exists
      # to remove (test-matrix.md §2).
      env:
        KIVYFORGE_REQUIRE_TOOLCHAIN: '1'
      run: |
        apk="$APP_DIR/hello-android-android/app/build/outputs/apk/release/app-release.apk"
        pytest tests/platforms/android/test_apk_signature.py -q --no-cov \
          --android-apk "$apk" --android-project "$APP_DIR"
    - name: Upload the generated project on failure (for triage)
      if: failure()
      uses: actions/upload-artifact@v6
      with:
        name: android-generated-project
        path: |
          ${{ env.APP_DIR }}/hello-android-android/app/build.gradle
          ${{ env.APP_DIR }}/hello-android-android/settings.gradle
          ${{ env.APP_DIR }}/hello-android-android/app/src/main/AndroidManifest.xml
          ${{ env.APP_DIR }}/hello-android-android/app/build/reports/
          ${{ env.APP_DIR }}/hello-android-android/app/build/kivyforge/

  # The macOS desktop gate: `kivyforge package -p macos` end to end against a
  # real ad-hoc signature, then the T3 artifact pass over what it produced.
  # Until this job existed, no CI job ran `kivyforge build`/`package` for ANY
  # desktop target — Linux, macOS and Windows were all proven by local runs
  # logged in test-matrix.md §7 and nothing that repeats (§3.2).
  # `macos_integration` below runs the hermetic suite only.
  #
  # No signing identity is configured, so the ad-hoc floor applies
  # (kivyforge/platforms/macos/cli.py: `package` requires no certificate,
  # keychain, or secret without one) — everything a CI runner has and
  # nothing more, the same reason `linux_appimage` needs no signing.
  #
  # It builds a fixture, not an example. tests/fixtures/apps/macos-gate commits
  # its pylock.macos.toml, which examples/** deliberately never does; see that
  # tree's README and test-matrix.md §5.3 (the reasoning `linux_appimage`
  # already established — a gate needs the same inputs every run).
  macos_app:
    name: macOS App (package + T3)
    runs-on: macos-latest
    env:
      APP_DIR: tests/fixtures/apps/macos-gate
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python 3.13
      # Pinned, per test-matrix.md §5.8: this is the sole CI proof of macOS
      # packaging, so it must not test whichever minor the runner image
      # happens to ship.
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
    - name: Install kivyforge
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: Doctor (macOS)
      # Gating, deliberately — no `continue-on-error` here. Every
      # signing-related check SKIPs rather than FAILs when no identity is
      # configured (the ad-hoc floor), so nothing here needs a certificate;
      # unlike android_gradle there is no known-interim WARN to waive either.
      working-directory: ${{ env.APP_DIR }}
      run: kivyforge doctor -p macos
    - name: Verify the committed lock still matches pyproject.toml
      # Meaningful here precisely because the lock is committed — against a
      # gitignored one it can only ever report "out of date"
      # (03-lockfile-concept.md §"Example-repo lock policy").
      working-directory: ${{ env.APP_DIR }}
      env:
        # The PBS runtime lookup queries GitHub's API, which is 60
        # requests/hour per IP unauthenticated; the token Actions
        # already provides raises that to 5000 and costs nothing.
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: kivyforge lock -p macos --check
    - name: Package the .app (T2, ad-hoc signed)
      # No --update: the committed lock is the input. If this step starts
      # failing without the fixture having changed, a pinned wheel or the PBS
      # runtime moved at its URL, which is worth knowing loudly.
      working-directory: ${{ env.APP_DIR }}
      run: kivyforge package -p macos
    - name: Assert the .app is the artifact the build promised (T3)
      # --macos-project turns on the Info.plist-vs-config comparison, which
      # had never run against a real bundle before this job (test-matrix.md
      # §5.1) — every prior run passed no expectations to compare against.
      env:
        KIVYFORGE_REQUIRE_TOOLCHAIN: '1'
      run: |
        app="$(ls -d "$APP_DIR"/build/macos/*.app)"
        pytest tests/platforms/macos/test_app_artifact.py -q --no-cov \
          --macos-app "$app" --macos-arch arm64 --macos-stripped \
          --macos-project "$APP_DIR"
    - name: Upload the app's Info.plist on failure (for triage)
      if: failure()
      uses: actions/upload-artifact@v6
      with:
        name: macos-gate-app-plist
        path: ${{ env.APP_DIR }}/build/macos/*.app/Contents/Info.plist

  # Android T3 from a *Windows* host (test-matrix.md §5.2). android_gradle
  # builds the same app on ubuntu, where `find_interpreter()` reaches a
  # versioned `python3.14` directly. Item 1's bug lived in what that search
  # does on **Windows** — the PEP 397 `py` launcher, versioned executables,
  # and rejecting a pre-release of the right minor — and nothing automated
  # ever built an APK there and inspected it.
  #
  # kivyforge runs under **3.13** here while the project ships 3.14, on
  # purpose. Run under 3.14, the resolver returns at its "use the running
  # interpreter" fast path and never searches, which is what this job did
  # until 2026-09-24 (it logged `resolver picked: ()`). Under 3.13 the build
  # has to find 3.14 itself, so the APK's .pyc magic check below proves the
  # *searched* interpreter wrote them.
  #
  # Release only, unlike android_gradle's debug+release. byte_compile and
  # strip_source are release-only tri-states, so the release path is the one
  # that invokes the resolver at all; a debug build here would cost Gradle
  # time on Windows and exercise nothing this job exists for.
  android_windows_host:
    name: Android T3 from a Windows host
    runs-on: windows-latest
    env:
      APP_DIR: examples/mobile/hello-android
      ABI: x86_64
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    # Three interpreters, set up in this order because each setup-python
    # prepends to PATH and the last one becomes the default `python`:
    #   3.15 pre-release: the *only* 3.15, so `py -3.15` answers with a
    #                     pre-release the resolver must reject (roadmap item
    #                     1's trap: a 3.14.0a7 once passed a bare version
    #                     check and wrote bytecode 3.14.6 refused to import);
    #   3.14 final:       what the project ships; the resolver must find it by
    #                     searching, and T3 runs under it because the magic
    #                     check reads its own MAGIC_NUMBER;
    #   3.13:             the default, and what kivyforge itself runs under.
    - name: Set up a Python 3.15 pre-release (to be rejected)
      # Pinned to an exact pre-release, not '3.15' + allow-prereleases: that
      # floats to 3.15.0 final once it ships (PEP 790: 2026-10-01), and the
      # rejection test would then fail its own precondition for reasons that
      # have nothing to do with the change under test. It will need a new pin
      # if the runner image ever preinstalls a 3.15 final, since `py -3.15`
      # would prefer it; the test says so when that happens.
      uses: actions/setup-python@v6
      with:
        python-version: '3.15.0-rc.2'
        allow-prereleases: true
    - name: Set up Python 3.14 (shipped; found by search; runs T3)
      id: py314
      uses: actions/setup-python@v6
      with:
        python-version: '3.14'
    - name: Set up Python 3.13 (runs kivyforge)
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: '17'
    - name: Set up the Android SDK
      uses: android-actions/setup-android@v4
      with:
        packages: platform-tools
    - name: Install kivyforge (with the Android extra)
      # Twice: under the default 3.13, which runs the build, and under 3.14,
      # which runs the T3 step (see the setup-python comment above).
      env:
        PY314: ${{ steps.py314.outputs.python-path }}
      run: |
        python -m pip install --upgrade pip
        python -m pip install -e ".[dev,android]"
        & $env:PY314 -m pip install -e ".[dev]"
    - name: Install the pinned NDK, CMake, platform, and build-tools
      # pwsh, not bash, and only for this step. `sdkmanager` on Windows is
      # `sdkmanager.bat`, and Git Bash does not apply PATHEXT — so a bare
      # `sdkmanager` is "command not found" there even though the SDK action
      # put it on PATH. The rest of this job stays on bash because the tools
      # those steps call (keytool, kivyforge, pytest) are .exe, which bash
      # does resolve.
      shell: pwsh
      run: |
        $ndk = python -c 'from kivyforge.platforms.android.toolchain import NDK_VERSION; print(NDK_VERSION)'
        sdkmanager --install "ndk;$ndk" "cmake;3.22.1" "platforms;android-35" "build-tools;35.0.0"
    - name: Assert the Windows resolver searches, finds and rejects correctly
      # The live half of test-matrix §5.2, which test_pycompile.py can only
      # cover with subprocess mocked. Under 3.13: `py -3.14` must win the
      # search, and a 3.15 whose only install is a pre-release must be
      # rejected. Each test fails (never skips) if its precondition is not
      # met, so a mis-set-up host cannot pass vacuously. `py --list` first,
      # so a failure shows what the launcher could see.
      run: |
        py --list
        python -m pytest tests/bundle/test_resolver_live.py -v --no-cov -s --resolver-found-minor 3.14 --resolver-prerelease-minor 3.15
    - name: Package the release APK (byte-compiled + stripped)
      # `package` rather than `build --debug`: byte_compile/strip_source are
      # release-only, so the release path is the one that reaches the
      # resolver. kivyforge runs under 3.13 here, so the 3.14 it compiles
      # with is the one the step above proved the search finds.
      shell: bash
      working-directory: ${{ env.APP_DIR }}
      env:
        KIVYFORGE_KEYSTORE_PASSWORD: ci-not-a-secret
      run: |
        keytool -genkeypair -noprompt -keystore ci.keystore -alias ci \
          -storepass "$KIVYFORGE_KEYSTORE_PASSWORD" \
          -keypass "$KIVYFORGE_KEYSTORE_PASSWORD" \
          -keyalg RSA -keysize 2048 -validity 30 -dname "CN=kivyforge CI"
        kivyforge package -p android --abi "$ABI" \
          --keystore ci.keystore --key-alias ci
    - name: Assert the Windows-built APK is what the build promised (T3)
      # The same three checks android_gradle runs, against an APK produced by
      # the Windows code path. --android-stripped is what makes the .pyc magic
      # assertion fire, which is item 1's actual failure mode. Under 3.14,
      # not the default 3.13: the magic check compares against the running
      # interpreter's MAGIC_NUMBER, and refuses a mismatched runner outright.
      shell: bash
      env:
        KIVYFORGE_REQUIRE_TOOLCHAIN: '1'
        PY314: ${{ steps.py314.outputs.python-path }}
      run: |
        apk="$APP_DIR/hello-android-android/app/build/outputs/apk/release/app-release.apk"
        manifest="$APP_DIR/hello-android-android/app/build/kivyforge/AndroidManifest-merged-release.xml"
        "$PY314" -m pytest tests/platforms/android/test_apk_artifact.py \
               tests/platforms/android/test_merged_manifest.py \
               tests/platforms/android/test_apk_signature.py \
          -q --no-cov \
          --android-apk "$apk" --android-abi "$ABI" --android-stripped \
          --android-merged-manifest "$manifest" --android-project "$APP_DIR"
    - name: Upload the generated project on failure (for triage)
      if: failure()
      uses: actions/upload-artifact@v6
      with:
        name: android-windows-host-project
        path: |
          ${{ env.APP_DIR }}/hello-android-android/app/build.gradle
          ${{ env.APP_DIR }}/hello-android-android/app/build/kivyforge/

  # T4, the last tier to reach CI: the app actually starts on a device and the
  # runtime contract holds. android_gradle proves the toolchain consumes our
  # output and the APK is shaped right; nothing before this job proved the
  # result *runs* (test-matrix.md §5.4).
  #
  # `run --smoke` is a kivyforge-generated instrumented test, so it asserts the
  # things only a running app can show: the SDL/python load order, the
  # extension-module finder, and a pyjnius PythonJavaClass proxy round-tripping
  # through invoke0 (android/06 §--smoke).
  #
  # --release on purpose, not --smoke alone. The release variant is
  # byte-compiled, stripped and R8-processed, so this is the tier that would
  # have caught roadmap item 1 — a stripped bundle the device refuses to
  # import — without a human holding a phone. A debug smoke run would exercise
  # the load model but not the stripping, which is the part with the history.
  # android/06 calls exactly this the "required release gate".
  #
  # Cost: ~10 min, the slowest job here. If that becomes a problem the natural
  # move is a `schedule:` trigger rather than dropping the tier.
  android_emulator:
    name: Android smoke test (emulator, T4)
    runs-on: ubuntu-latest
    env:
      APP_DIR: examples/mobile/hello-android
      ABI: x86_64
      API_LEVEL: '35'
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python 3.14
      # Load-bearing, same as android_gradle's: --release byte-compiles the
      # payload, and `find_interpreter` needs a *final* CPython matching the
      # minor this project ships (3.14) or the build degrades to shipping
      # source with a warning — which would quietly turn this into a debug-
      # equivalent run and stop testing the thing it exists to test.
      uses: actions/setup-python@v6
      with:
        python-version: '3.14'
    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: '17'
    - name: Set up the Android SDK
      uses: android-actions/setup-android@v4
      with:
        packages: platform-tools
    - name: Install kivyforge (with the Android extra)
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev,android]"
    - name: Install the pinned NDK, CMake, platform, and build-tools
      run: |
        ndk="$(python -c 'from kivyforge.platforms.android.toolchain import NDK_VERSION; print(NDK_VERSION)')"
        sdkmanager --install "ndk;${ndk}" "cmake;3.22.1" "platforms;android-35" \
          "build-tools;35.0.0"
    - name: Enable KVM
      # Hosted ubuntu runners do expose /dev/kvm, but only to the `kvm` group;
      # without this rule the emulator falls back to software emulation and
      # takes long enough to hit the job timeout. This is the standard
      # incantation, not a kivyforge-specific workaround.
      run: |
        echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
          | sudo tee /etc/udev/rules.d/99-kvm4all.rules
        sudo udevadm control --reload-rules
        sudo udevadm trigger --name-match=kvm
    - name: Boot an AVD and run the contract smoke test (T4)
      uses: reactivecircus/android-emulator-runner@v2
      with:
        api-level: ${{ env.API_LEVEL }}
        # google_apis rather than the bare AOSP image: it is what the local
        # AVD this was rehearsed against uses, and the bare image omits
        # services some Android APIs assume are present.
        target: google_apis
        arch: x86_64
        # -no-window/-no-audio: nothing here renders to a screen, the probe is
        # an instrumented test. -no-snapshot keeps each run starting from the
        # same state, so a green run cannot depend on a cached boot.
        emulator-options: >-
          -no-window -no-audio -no-boot-anim -no-snapshot
          -gpu swiftshader_indirect -camera-back none
        # One line, chained with &&, on purpose. This action runs **each line
        # of `script` as its own `sh -c`**, so a `cd` on its own line applies
        # to a shell that then exits and the next line starts back at the
        # repo root. That failed exactly once, with kivyforge reporting
        # "missing [tool.kivy] table" against kivyforge's own pyproject.toml
        # rather than the example's. Keep it a single command.
        script: >-
          cd "$APP_DIR" &&
          kivyforge run --smoke --release -p android --abi "$ABI"
    - name: Upload the instrumentation report on failure (for triage)
      if: failure()
      uses: actions/upload-artifact@v6
      with:
        name: android-smoke-report
        path: |
          ${{ env.APP_DIR }}/hello-android-android/app/build/reports/androidTests/
          ${{ env.APP_DIR }}/hello-android-android/app/build/outputs/androidTest-results/

  # macOS integration: the full suite runs here too (pbxproj generation and
  # xcodebuild/simctl command construction get real-macOS coverage). The live
  # `kivyforge build --simulator` / `kivyforge run` smoke test against a real
  # iOS app is `ios_simulator`, below — it was deferred here pending the iOS
  # Python.xcframework + wheels being published (spec 03/04); that happened
  # 2026-07-27 (test-matrix.md §3.2), so this job now exists as its own step
  # rather than folded into this one, mirroring how `macos_app` sits beside
  # this job instead of inside it.
  macos_integration:
    name: Integration (macOS)
    runs-on: macos-latest
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python 3.13
      # Pinned 2026-09-23, closing test-matrix.md §5.8. This job ran '3.x' —
      # whichever minor the runner image happened to ship — while being the
      # only host in the system that compiles the macOS launcher with a real
      # clang. A macOS-specific break on one minor versus another was
      # therefore invisible, and the interpreter could change under us the
      # way the MSVC toolset did three times (§7).
      #
      # 3.13 matches macos_app, so the two macOS jobs agree; the hermetic
      # suite's own 3.13/3.14 matrix coverage comes from unit_tests.
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
    - name: Install kivyforge and test dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: Run pytest
      # This is the only job that can build the Mach-O launcher, and those tests
      # self-skip when clang is absent — so require the toolchain rather than
      # letting the runner image quietly drop the one thing this job adds.
      env:
        KIVYFORGE_REQUIRE_TOOLCHAIN: '1'
      run: pytest -q

  # The iOS simulator gate: roadmap item 5 asked for this "when the wheels it
  # waits on are published" — they were, 2026-07-27, and this closes that out.
  # No signing, no cert, no team_id: --simulator never signs (spec 05), so
  # this needs nothing a CI runner doesn't already have, the same reason
  # macos_app needs no certificate for its ad-hoc floor.
  #
  # This runs `examples/mobile/hello-kivy` directly rather than a
  # tests/fixtures/apps fixture, the same choice android_gradle made:
  # hello-kivy is one of the three on-device gate examples whose
  # pylock.ios.toml is deliberately committed (AGENTS.md, this example's own
  # .gitignore) specifically so a CI job has a determinate lock to build
  # from without re-resolving it.
  #
  # T3 harness added 2026-09-23 (ios-t3-checks-prompt.md / test-matrix.md
  # §5.1): required entries, a whole-bundle Mach-O arch sweep, and the
  # Info.plist-vs-config comparison. No stripped/`.pyc`-magic pass — iOS
  # `strip_source` cannot be exercised honestly yet (see
  # tests/artifact_checks.py's iOS module note) — and no separate signature
  # step: `--simulator` never signs with a real identity (spec 05), so
  # `codesign --verify`'s only interesting outcome here is "ad-hoc still
  # verifies", which `test_the_app_is_codesigned` already covers every push.
  ios_simulator:
    name: iOS Simulator (build + run)
    runs-on: macos-latest
    env:
      APP_DIR: examples/mobile/hello-kivy
    steps:
    - name: Checkout kivyforge
      uses: actions/checkout@v6
    - name: Set up Python 3.13
      # Pinned for the §5.8 reason, not the byte-compile one. Correct that
      # plain `build`/`run` never byte-compiles, so no *magic number* has to
      # match here — but that is `android_gradle`'s argument, not §5.8's.
      # §5.8 is about host drift: this is the only job that exercises the iOS
      # code paths against a real Xcode, and every one of its steps runs
      # kivyforge itself under this interpreter, so an iOS-path break on one
      # minor versus another would be invisible while the image chooses.
      #
      # 3.13 matches macos_app and macos_integration, so all three macOS-runner
      # jobs agree. The iOS *target* runtime is the vendored
      # Python.xcframework and is unaffected by this.
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
    - name: Install kivyforge
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
    - name: Doctor (iOS)
      # Gating, deliberately. auto_signing = true and an empty
      # provisioning_profile/entitlements set means every signing-related
      # check PASSes or SKIPs on a bare runner — nothing here needs a waiver.
      working-directory: ${{ env.APP_DIR }}
      run: kivyforge doctor -p ios
    - name: Verify the committed lock still matches pyproject.toml
      working-directory: ${{ env.APP_DIR }}
      run: kivyforge lock -p ios --check
    - name: Build for the simulator (T2)
      working-directory: ${{ env.APP_DIR }}
      run: kivyforge build -p ios --simulator
    - name: Assert the .app is the artifact the build promised (T3)
      # --ios-project turns on the Info.plist-vs-config comparison, mirroring
      # macos_app's own --macos-project step — the first time either check
      # has run against a real built .app. Not run from working-directory
      # ${{ env.APP_DIR }}, the same reason android_gradle's/linux_appimage's
      # T3 steps aren't: `tests/` is repo-root-relative.
      env:
        KIVYFORGE_REQUIRE_TOOLCHAIN: '1'
      run: |
        app="$(ls -d "$APP_DIR"/hello-kivy-ios/build/DerivedData/Build/Products/Debug-iphonesimulator/*.app)"
        pytest tests/platforms/ios/test_app_artifact.py -q --no-cov \
          --ios-app "$app" --ios-arch arm64 --ios-project "$APP_DIR"
    - name: Install + launch on the simulator (T4 launch smoke)
      # resolve_simulator_destination boots a default device itself when none
      # is running, so this needs no prior `simctl boot` step.
      #
      # Backgrounded rather than run in the foreground: `simctl launch`'s
      # `--console-pty` (kivyforge/platforms/ios/xcode/runner.py) streams the
      # app's console forever once it launches — correct for a human watching
      # a real device, but there is no "confirm launched, then detach" verb,
      # so a foreground run here would never return and the step would hang
      # until the job timeout. Still running after the wait is the launch
      # succeeding; exiting early is `simctl launch` raising, exactly as it
      # would have printed in the foreground. Confirmed locally (2026-09-23)
      # against a real `iPhone Air` simulator before writing it this way.
      working-directory: ${{ env.APP_DIR }}
      run: |
        kivyforge run -p ios --simulator --no-build > run.log 2>&1 &
        run_pid=$!
        sleep 15
        if ! kill -0 "$run_pid" 2>/dev/null; then
          echo "kivyforge run exited early -- install or launch failed:"
          cat run.log
          exit 1
        fi
        echo "Still running after 15s (installed and launched):"
        cat run.log
    - name: Screenshot the running app
      # Confirms the T4 bar test-matrix.md names — "the app reaches its first
      # frame" — visually, the same evidence the local validation sessions
      # captured by hand (ios-validation-findings.md and friends). No
      # assertion reads this file; it is for a human to open from the run.
      working-directory: ${{ env.APP_DIR }}
      run: |
        sleep 3
        xcrun simctl io booted screenshot hello-kivy-simulator.png
    - name: Upload the simulator screenshot
      if: always()
      uses: actions/upload-artifact@v6
      with:
        name: ios-simulator-screenshot
        path: ${{ env.APP_DIR }}/hello-kivy-simulator.png
        if-no-files-found: ignore
    - name: Upload the generated project on failure (for triage)
      if: failure()
      uses: actions/upload-artifact@v6
      with:
        name: ios-generated-project
        path: |
          ${{ env.APP_DIR }}/hello-kivy-ios/hello-kivy.xcodeproj/project.pbxproj
          ${{ env.APP_DIR }}/hello-kivy-ios/build/DerivedData/Build/Products/**/Info.plist
